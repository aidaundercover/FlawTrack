import 'package:flutter/material.dart';
import 'package:flawtrack/views/forum.dart';
import 'package:flawtrack/views/maps.dart';
import 'package:flawtrack/views/volunteer.dart';
import 'package:flawtrack/views/profile.dart';
import 'package:flawtrack/views/forum.dart';


class Maps extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}

class Forum extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}

class Profile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}

class Volunteers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}